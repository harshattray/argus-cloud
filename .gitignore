node_modules/
dist/
.env
.env.*
*.log
test/.tmp-*
.pgdata/
.next/
.next-dev/
.next-verify/
web/node_modules/
.DS_Store
web/tsconfig.tsbuildinfo
.storage/
.vercel

# Fontconfig scratch for scripts/outline_svg_text.py — a downloaded brand face
# and a config holding absolute paths to one checkout. The scripts run without
# it; only the outlined output is committed.
.font-cache/

# Generated by scripts/embed-migrations.mjs on every build.
src/migrations.generated.ts
web/lib/legal.generated.ts

# Calibration run output. Regenerate with `npm run calibrate:hosted` — the
# figures that matter are transcribed into docs/calibration.md, which is the
# record; this is the raw dump behind them.
.calibrate-hosted.json
.calibrate-tmp/
.calibrate-crops.cjs
